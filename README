Simplified frp extracted from freja by saikyun

Via a terminal, start by: `janet main.janet`

Move mouse in window, type keys, etc. while examining terminal output.

Consider looking at `main.janet` and `frp.janet` for comprehension.

